import { featureFlag } from '../../../helpers/feature-flag';
import { module, test } from 'qunit';

module('Unit | Helper | feature flag');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = featureFlag([42]);
  assert.ok(result);
});
